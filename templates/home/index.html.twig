{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <h1 class="text-4xl font-bold text-teal-500 mb-4">
        Bonjour, {{ app.user ? app.user.name|upper : 'VISITEUR' }}
    </h1>

    <p class="text-gray-700 mb-6">
        Bienvenue sur le site de gestion de projets.
    </p>

    {% if app.user %}
        <a href="{{ path('app_logout') }}"
           class="inline-block px-6 py-2 bg-red-500 text-white rounded hover:bg-red-600 transition">
            Se déconnecter
        </a>
    {% else %}
        <a href="{{ path('app_login') }}"
           class="inline-block px-6 py-2 bg-teal-500 text-white rounded hover:bg-teal-600 transition">
            Se connecter
        </a>
    {% endif %}
{% endblock %}
