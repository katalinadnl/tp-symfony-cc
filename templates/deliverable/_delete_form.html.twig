<form method="post" action="{{ path('app_deliverable_delete', {'id': deliverable.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ deliverable.id) }}">
    <button class="btn">Delete</button>
</form>
